<Window
	x:Class="Sudoku.Graphics.UI.MainWindow"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:cmd="clr-namespace:Sudoku.Graphics.UI.Commands"
	xmlns:vm="clr-namespace:Sudoku.Graphics.UI.ViewModels"
	xmlns:p="clr-namespace:Sudoku.Graphics.UI.Properties"
	mc:Ignorable="d"
	Title="{x:Static p:Resources.MainWindow_Title}" Height="600" Width="800">
	<Window.DataContext>
		<vm:MainWindowViewModel x:Name="ViewModel"/>
	</Window.DataContext>

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition/>
		</Grid.RowDefinitions>
		<Menu Grid.Row="0">
			<MenuItem Header="{x:Static p:Resources.MainWindow_MenuItem_File}">
				<MenuItem Header="{x:Static p:Resources.MainWindow_MenuItem_Create}" InputGestureText="Ctrl+N"/>
				<Separator/>
				<MenuItem
					Header="{x:Static p:Resources.MainWindow_MenuItem_Quit}" InputGestureText="Ctrl+W"
					Command="{Binding QuitCommand}"
					CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"/>
			</MenuItem>
			<MenuItem
				Header="{x:Static p:Resources.MainWindow_MenuItem_About}"
				Command="{Binding OpenAboutWindowCommand}"
				CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}">
			</MenuItem>
		</Menu>
	</Grid>
</Window>
